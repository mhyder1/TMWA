{
  "name": "remote",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "serve:dist": "npm run build && npx live-server dist --entry-file=index.html --host=0.0.0.0 --port=8080"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "dayjs": "^1.11.20",
    "glightbox": "^3.3.1",
    "preact": "^10.24.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.8"
  }
}
